#include "main.h"
#include <stdio.h>
/**
 * _islower - checks for lowercase character
 * @c: Input character
 * Description: _putchar prints in lowercase
 * Return: lowercase
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
