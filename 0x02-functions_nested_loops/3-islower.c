#include "main.h"
/**
 * _islower - checks for lowercase character
 *
 * Return: Always 1 if it is lowercase and 0 if is uppercase
 */
int _islower(int c)
{
	char x;
	int lower = 0;

	for (x = 'a'; x <= 'z'; x++)
	{
		if (x == c)
			_putchar('x');
		else if (x != c)
	}
	retrn (0);
}
