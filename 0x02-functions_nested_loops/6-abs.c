#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @x: An integer input
 * Return: Always 0 (Success)
 */
int _abs(int)
{
	if (x >= 0)
		return (x);
	else
		return (* -1);
}
